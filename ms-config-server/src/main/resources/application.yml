server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maiksurco/BACKEND2023-2.git
          #username:
          #password:
          search-paths: config-data
          default-label: ms-registry-server
  security:
    user:
      name: root
      password: s3cr3t